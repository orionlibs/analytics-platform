---
cluster:
  name: servicemonitor-invalid-selector-test

externalServices:
  prometheus:
    host: https://prometheus.example.com
    basicAuth:
      username: "12345"
      password: "secret"

metrics:
  enabled: true
  serviceMonitors:
    enabled: true
    selector: 'invalid_selector_format_here'

logs:
  enabled: false
  pod_logs:
    enabled: false
  cluster_events:
    enabled: false