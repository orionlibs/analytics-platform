---
section:
  # We don't want to include the examples, just the fact that this is an object
  objectWithExamples:
    example1:
      kind: Example
      name: Example1
    example2:
      kind: Example
      name: Example2
